import PollingFinder from '../components/polling/PollingFinder';

/**
 * FinderPage — Contains the Polling Place Finder module.
 */
export default function FinderPage() {
  return <PollingFinder />;
}
